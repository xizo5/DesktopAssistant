# 桌面助手 v1.0.0

把别的程序窗口**真实收编**进一个主窗口统一管理的 Windows 工具：格子永远填满、拖分隔条实时自适应、随时弹回桌面原样、退出时绝不关闭任何程序。

不是截图也不是录屏——是真嵌入，收进来的程序照常运行、照常交互。

## 功能

- **真嵌入**：任意程序窗口变成主窗口里的一格，照常交互
- **填满式布局**：格子拼满窗口；拖分隔条实时重排；模板一键铺开（左右各半 / 1大2小 / 田字格）
- **换位置**：右键格子标题条，与其他格子整体互换（窗口跟着格子走）
- **托盘常驻**：最小化收进托盘不闪现；托盘菜单可找回主窗口、一键弹出全部窗口
- **自愈**：上次异常残留的"孤儿窗口"收编时自动修复
- **绝不关程序**：弹出还原到桌面原位，退出时全部自动弹出

## 使用说明

- 双击 `DesktopAssistant.exe` 即可，无需安装 Python
- 首次运行如遇 Windows 蓝色 SmartScreen 提示：点「更多信息」→「仍要运行」
- 建议把 exe 放在桌面或某个普通文件夹里再运行（配置存在 exe 旁边）
- 以管理员权限运行的程序、UWP 应用收不动（Windows 系统限制）
- 需要 Windows 10 / 11（64 位）

## 已知边界

- 个别自绘界面的程序可能显示异常
- 收编后的窗口不再出现在任务栏/Alt+Tab（真嵌入的正常表现）
