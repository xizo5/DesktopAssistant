# 桌面助手 (Desktop Assistant)

一个 Windows 桌面工具：把用户选定的第三方程序窗口真实地嵌入到一个主窗口里统一管理，并让所有嵌入窗口以"填满式"自适应布局排列。

## Language

**Host（主窗口）**:
承载所有嵌入窗口的那个应用窗口。
_Avoid_: 容器、壳、母窗口

**Guest（嵌入窗口）**:
被真实嵌入到 Host 里的一个第三方程序窗口。
_Avoid_: 子窗口、目标窗口、嵌入项

**Tile（瓦片）**:
Host 内的一块矩形区域，恰好容纳一个 Guest。Tile 的尺寸变化 = 布局重排。
_Avoid_: 格子、面板、槽位

**Fill Layout（填满式布局）**:
Host 内所有 Tile 拼满整个可用区域、不留缝隙的排布方式。拖动分隔条会按邻居规则重排所有 Tile。
_Avoid_: 自由布局、悬浮模式

**Settle（收编）**:
通过窗口列表把一个正在运行的第三方窗口变成本应用 Guest 的动作。
_Avoid_: 捕获、导入、添加

**Eject（弹出还原）**:
把 Guest 从 Host 中释放回桌面、恢复为普通独立窗口的动作。永远不关闭 Guest 程序本身。
_Avoid_: 移除、关闭、释放

**Template（模板）**:
预设的 Tile 排布方案（如左右各半、1 大 2 小、田字格），作为快速布满的快捷方式，可在其基础上拖动分隔条自定义。
_Avoid_: 预设、方案
